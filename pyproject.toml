[project]
name = "schemes"
version = "1.0.0"
dependencies = [
    "flask"
]

[project.optional-dependencies]
dev = [
    "playwright",
    "pytest",
    "pytest-flask",
    "pytest-playwright",
    "mypy"
]

[tool.mypy]
strict = true
